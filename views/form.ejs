<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <nav>
    <h1><%= item._id ? "Edit Item" : "Add New Item" %></h1>
  </nav>
  <form action="<%= item._id ? `/edit/${item._id}` : '/create' %>" method="POST">
    <label>Title</label>
    <input type="text" name="title" value="<%= item.title || '' %>">
    <label>Description</label>
    <textarea name="description"><%= item.description || '' %></textarea>
    <label>Image 1 URL</label>
    <input type="text" name="image1" value="<%= item.image1 || '' %>">
    <label>Image 2 URL</label>
    <input type="text" name="image2" value="<%= item.image2 || '' %>">
    <label>Image 3 URL</label>
    <input type="text" name="image3" value="<%= item.image3 || '' %>">
    <button type="submit"><%= item._id ? "Update" : "Create" %></button>
  </form>
</body>
</html>
